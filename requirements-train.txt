# Training-only extras NOT already in requirements.txt (mlflow, lightgbm,
# xgboost, shap, etc. are already part of the base serving requirements --
# shap moved there since api/score.py now computes real per-instance SHAP
# at serving time, not just during training).
# Install with: pip install -r requirements.txt -r requirements-train.txt
-r requirements.txt

# Tabular foundation models, evaluated for the credit ensemble but not
# currently wired into models/registry.py's champion pattern -- see
# docs/data-schema-cr.md for status. TabPFN requires a HuggingFace license
# token (TABPFN_TOKEN) to download its classifier checkpoint; Nori's
# checkpoint is public.
tabpfn>=2.0.0
synthefy-nori>=0.3.0

# torch is a transitive dep of tabpfn/synthefy-nori. On very recent GPUs
# (e.g. RTX 50xx / Blackwell) the stable PyPI wheel may not include the
# right CUDA kernels yet -- both libraries fall back to CPU automatically.
torch>=2.4.0
pyarrow>=17.0.0
